# Configured to run TSLint.
image: node:10.15.3

pipelines: 
  branches:
    master:
      - step:
          name: Run TS Lint
          caches: 
            - node
          script: 
            - npm --version
            - npm install
            - npm run pipeline_lint